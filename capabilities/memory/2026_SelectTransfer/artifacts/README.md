# Artifacts

这里存放生成出来的 memory artifacts。

建议结构：

- 每个 `source_set_id` 一个文件夹
- 每个文件夹内部至少包含：
  - `episodic_trace.md`
  - `cross_episode_consolidation.md`

artifact 命名尽量保持稳定，方便后面的 run pipeline 直接按文件名引用。
